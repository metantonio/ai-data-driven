okay, todo funciona perfectamente, por lo que la nueva funcionalidad que quiero implementar debe usar un nuevo endpoint para no tocar lo que ya existe.
En el dashboard se debe agregar un checkbox que diga "Advanced Analysis", dicho botón hará básicamente lo mismo que ya hace la aplicación, la diferencia es que ahora habrá el usuario navegará a una página donde aparecen las tablas de la base de datos, con las columnas y el tipo de data, pero lo nuevo es que agregaremos "una columna adicional que se llame" "User Comments" donde el usuario podrá agregar comentarios sobre el significado de dicha columna, así de esta manera el agente de IA podrá interpretar mejor el significado de dicha columna para el análisis y creación de los modelos de ML. Finalmente, después de enviados los comentarios, deberíamos volver a la vista de Results. 
Por lo tanto, haz los cambios necesarios para que este nuevo endpoint funcione correctamente y que sea compatible con la funcionalidad que ya existe (tal vez haya que agregar propiedades a la funcionalidad anterior para que funcione con la nueva).